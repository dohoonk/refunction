<% if @testimonial.errors.any? %>
  <div id="error_explanation">
   <h2><%= pluralize(@testimonial.errors.count, "error") %> prohibited this testimonial from being saved:</h2>

   <ul>
   <% @testimonial.errors.full_messages.each do |msg| %>
     <li><%= msg %></li>
   <% end %>
   </ul>
  </div>
<% end %>

<%= form_for(@testimonial) do |f| %>
<div class="container card-inner-padding">
  <div class="row justify-content-between mg-left card-margin-bottom">
    <div>
      <span class="cta-see-team">New Testimonial</span>
    </div>
  </div>
  <div class="row">
    <!-- <div class="col-sm-6">
      <div class="admin-member-pic-container">

      </div>
    </div> -->

    <div class="col-sm-12">
      <div class="row">
        <div class="col-sm-4 margin-bottom-11">
          <%= f.text_field :first_name, class:"form-control input-box", placeholder: "First Name" %>
        </div>
        <div class="col-sm-4 margin-bottom-11" >
          <%= f.text_field :last_name, class:"form-control input-box", placeholder: "Last Name" %>
        </div>
        <div class="col-sm-4 margin-bottom-11" style="padding-left: 0px;">
          <%= f.text_field :title, class:"form-control input-box", placeholder: "Title" %>
        </div>
      </div>
    </div>
    <div class="col-sm-12 card-margin-top">
      <!-- <#%= f.text_area :description, :class => "tinymce", :rows => 40, :cols => 30 %>
      <#%= tinymce %> -->
      <%= f.text_area :description, :class => "form-control input-box", :rows => 40, :cols => 30 %>
    </div>
    <div class="col-sm-12 card-margin-top">
      <%= f.submit "Save", class:"admin-button-gray" %>
      <%= f.submit "Save & Publish", class:"admin-button-gray", value: "Save & Publish" %>
      <button class="admin-button-red">Delete</button>
    </div>
  </div>
<% end %>
